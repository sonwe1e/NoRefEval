# 面向 60→120 FPS 游戏插帧的无中间帧参考评测技术路线

你真正需要的不是一个普通的“无参考视频质量模型”，而是一套**端点参考的插帧质量评测系统**。虽然真实的 120 FPS 中间帧不存在，但原始 60 FPS 帧仍然是可靠锚点，因此这类任务更准确地说是：

> **Endpoint-Referenced / Reduced-Reference VFI Quality Assessment，端点参考插帧质量评测。**

系统不应试图猜测唯一的 Ground Truth，而应判断生成帧是否同时满足：

1. 可以被前后原始帧合理解释；
2. 位于合理的运动轨迹上；
3. 没有引入闪烁、撕裂、重影和结构缺失；
4. 人物、武器、细杆、UI、文字等关键对象保持稳定；
5. 对于本身无法从端点唯一确定的离散事件，给出较低置信度，而不是武断判错。

最终产物不应只是一个总分，而应是：

* 一个总体质量分；
* 七到八个可解释的子项分数；
* 置信度与事件歧义度；
* 最差时间段、问题区域和错误类型；
* 同一源视频下多个插帧模型的可靠排序。

已有主观研究表明，通用图像或视频质量指标对插帧失真的排序能力有限，BVI-VFI 中即使表现较好的通用指标，其与主观质量的相关性仍然不高。更关键的是，在面向渲染视频的 ReVQ-2k 消融实验中，如果不进行运动对齐，时序稳定性预测的 SRCC 约为 0.17；加入运动对齐和遮挡处理后可提高到约 0.84。这基本决定了整个技术路线：**运动补偿后的时序与结构一致性必须是主干，通用无参考 VQA 只能作为弱先验。** ([arXiv][1])

---

# 一、整体系统架构

设原始 60 FPS 视频为：

[
X_0,X_1,\ldots,X_N
]

插帧输出 120 FPS 视频为：

[
Y_{2i}=X_i,\qquad Y_{2i+1}=M_i
]

其中 (M_i) 是模型生成的 (X_i) 和 (X_{i+1}) 之间的中间帧。

建议系统采用如下结构：

```text
原始 60 FPS 视频
      │
      ├── 时间戳、颜色、分辨率和锚点对齐
      │
      ├── 源视频特征缓存
      │     ├── X_i → X_{i+1} 直接光流
      │     ├── 遮挡与可见性
      │     ├── 全局相机运动
      │     ├── 边缘、梯度和特征金字塔
      │     ├── UI / 人物 / 细物体区域
      │     └── 源视频轨迹与深度（按需）
      │
候选 120 FPS 视频
      │
      ├── 全帧低成本风险扫描
      │
      ├── 均匀采样 + 高风险片段采样
      │
      ├── 核心端点一致性评测
      │     ├── 光流组合一致性
      │     ├── 遮挡感知重建
      │     ├── 反向锚点闭环
      │     ├── 运动补偿时序残差
      │     └── 奇偶帧频率与清晰度稳定性
      │
      ├── 高风险区域语义诊断
      │     ├── 人物身体
      │     ├── 武器
      │     ├── 柱子和细小物体
      │     ├── UI 和文字
      │     └── 卡牌与商店状态切换
      │
      └── 单调融合模型
            ├── 总分
            ├── 分类子分
            ├── 置信度
            └── 坏例时间轴和可视化
```

最关键的工程原则是：**所有只依赖原始 60 FPS 视频的计算都缓存一次。**

评测 (N) 个插帧模型时，总成本应接近：

[
T(N)=T_{\text{source-cache}}+N\cdot T_{\text{candidate}}
]

而不是：

[
T(N)=N\cdot
\left(
T_{\text{source}}+T_{\text{candidate}}
\right)
]

对于同一批视频反复比较多个模型，这会是最显著的加速来源。

---

# 二、数据对齐与基础完整性检查

这一层看起来简单，但必须先完成，否则后面的光流和时序指标可能全部失真。

## 1. 按时间戳对齐，而不是只按帧号对齐

应通过视频的 PTS/DTS 确认：

[
Y_{2i}\leftrightarrow X_i
]

不能默认解码后第 (2i) 帧一定对应第 (i) 个源帧。需要处理：

* 可变帧率；
* 首帧偏移；
* 重复帧；
* 丢帧；
* 编码器造成的时间戳重排；
* 输出视频实际不是严格 120 FPS。

建议使用 PyAV 或 FFmpeg 获取时间戳，OpenCV 仅作为图像处理工具。

## 2. 锚点完整性检查

理论上偶数帧应该是原始帧：

[
E_{\text{anchor}}(i)=D(Y_{2i},X_i)
]

其中 (D) 应包含：

* Y 通道像素误差；
* 色度误差；
* 梯度误差；
* 感知特征误差。

如果视频经过重新编码，偶数帧不可能逐像素完全一致，因此需要先测量编码基线，然后把生成帧误差与该基线分离。

锚点检查能够发现：

* 输出帧错位；
* 插帧器修改了原始帧；
* 色彩范围错误；
* RGB/YUV 转换错误；
* 全局亮度、Gamma 或锐化发生变化。

## 3. 镜头切换单独处理

在镜头切换处不存在合理连续运动，应将其标记为 `scene_transition`，不要继续应用普通光流一致性指标。

对于镜头切换，只评估：

* 是否产生额外混合帧；
* 是否出现双重画面；
* 切换是否被延迟或提前；
* 是否出现切换后闪回。

---

# 三、核心指标一：端点光流组合一致性

这是整套方案中最重要的基础指标之一。

定义：

[
F_{01}=F(X_i,X_{i+1})
]

[
F_{0m}=F(X_i,M_i)
]

[
F_{m1}=F(M_i,X_{i+1})
]

对于一个合理的中间帧，从 (X_i) 经过 (M_i) 再到 (X_{i+1}) 的组合运动，应与直接从 (X_i) 到 (X_{i+1}) 的运动接近：

[
F_{0m}(x)
+
F_{m1}\left(x+F_{0m}(x)\right)
\approx
F_{01}(x)
]

因此定义归一化组合误差：

[
E_{\text{comp}}=
\frac{
\sum_x w(x),
\rho\left(
\frac{
\left|
F_{0m}(x)
+
F_{m1}\left(x+F_{0m}(x)\right)
------------------------------

F_{01}(x)
\right|*2
}{
|F*{01}(x)|_2+\tau
}
\right)
}{
\sum_x w(x)+\epsilon
}
]

其中：

* (\rho) 使用 Charbonnier 或 Huber，以降低异常光流的影响；
* (w(x)) 综合光流置信度、可见性和遮挡掩码；
* 反方向 (X_{i+1}\rightarrow M_i\rightarrow X_i) 也要计算；
* 最后使用双向误差的均值和高分位数。

这个指标能够直接捕捉：

* 背景在旋转时撕裂；
* 柱子跟随错误背景运动；
* 人物身体局部运动错误；
* 武器运动层归属错误；
* 中间帧局部冻结；
* 某些区域直接复制前帧或后帧。

## 遮挡必须排除

如果一个像素在端点之间发生遮挡或显露，它本来就不满足严格光流闭环。因此需要通过前后向光流一致性构造：

* 前向遮挡区域；
* 后向遮挡区域；
* 低置信区域；
* 仅能由一侧端点解释的区域。

核心组合误差只在高置信可见区域计算。遮挡区域进入单独的“显露区域质量”分支。

对渲染视频的研究已经证明，运动对齐和遮挡掩码不是可选项，而是时序质量评测能否有效的关键。([arXiv][2])

## 光流模型选择

建议分成两档：

* 标准评测使用 SEA-RAFT 一类准确且相对高效的光流模型；
* 全帧粗筛使用低分辨率轻量光流、块匹配或局部运动统计。

SEA-RAFT 的论文报告其在保持有竞争力精度的同时，相比当时多种方法具有明显速度优势，适合作为评测端的独立光流模型。([arXiv][3])

必须避免使用被评测插帧模型自己输出的光流作为主要评价依据，否则会出现“模型用自己的运动解释自己的错误”。候选模型内部的 flow、mask 和 reuse map 可以作为辅助诊断信息，但不能作为评测真值。

---

# 四、核心指标二：反向锚点闭环

这是最值得实现的一个增强指标。

相邻两个生成帧分别位于：

[
M_{i-1}: t=i-\frac12
]

[
M_i: t=i+\frac12
]

原始锚点 (X_i) 恰好位于它们的中间。因此可以通过一个独立的重建器 (\mathcal R)，从两个生成帧反向重建已知锚点：

[
\hat X_i=
\mathcal R(M_{i-1},M_i,0.5)
]

然后计算：

[
E_{\text{cycle}}=
D(\hat X_i,X_i)
]

这样就把一部分“无中间帧 Ground Truth”的问题，重新转化成了一个有 Ground Truth 的闭环问题。

它尤其能发现：

* 相邻生成帧彼此不一致；
* 奇数帧有系统性模糊；
* 人物结构在连续中间帧之间闪动；
* 武器在两帧之间跳变；
* UI 在生成帧子序列中持续漂移；
* 局部区域虽然分别看起来合理，但无法组成连续运动。

时间循环一致性已经被用于无监督视频插帧训练，说明从中间状态反向重建已知帧具有明确的约束价值。这里将它从训练约束转化为评测特征。([arXiv][4])

需要注意：

1. (\mathcal R) 必须与候选插帧模型独立；
2. 不建议只依赖一个大型 VFI 模型作为 (\mathcal R)；
3. 可以同时使用光流前向投影重建和一个轻量插帧重建器；
4. 应在真实高帧率数据上测量 (\mathcal R) 自身的误差基线；
5. 该指标应作为组合特征，而不是单独决定最终得分。

---

# 五、核心指标三：运动补偿后的时序稳定性

直接比较相邻帧差异没有意义，因为正常运动也会产生很大差异。正确做法是先把一个时序窗口中的帧对齐到中心帧，再分析残差。

以五帧窗口为例：

[
Y_{k-2},Y_{k-1},Y_k,Y_{k+1},Y_{k+2}
]

将其他帧运动补偿到 (Y_k)：

[
\tilde Y_{j\rightarrow k}
=========================

W(Y_j,F_{k\rightarrow j})
]

再计算运动补偿残差：

[
R_j=
\tilde Y_{j\rightarrow k}-Y_k
]

建议同时统计：

* RGB 或 Y 通道 Charbonnier 残差；
* 梯度残差；
* Laplacian 金字塔残差；
* 浅层感知特征残差；
* 残差的 P50、P90、P99；
* 遮挡外大残差区域的连通面积；
* 残差随时间的二阶变化。

这一分支主要评价：

* 人物身周模糊和拖影；
* 剑尖、剑柄闪烁；
* 背景纹理时序抖动；
* 柱子边缘摆动；
* UI 和文字的细微闪烁；
* 生成帧清晰度与原始帧交替变化。

ReVQ-2k 采用了类似的空间质量分支与运动对齐时序分支，并在渲染视频和实时帧生成策略比较中验证了其有效性；但该工作也明确指出，对运动自然度、复杂运动内容和运行速度仍存在局限，因此它适合作为本方案的基础，而不是完整答案。([arXiv][2])

---

# 六、核心指标四：奇偶帧交替与频率特性

在 120 FPS 输出中，偶数帧是原始帧，奇数帧是生成帧。一旦生成帧存在系统性模糊、锐化、亮度或结构差异，视频就会产生非常典型的奇偶交替。

在 120 FPS 时序中，一帧一变的奇偶交替对应接近 60 Hz 的时序分量。

## 1. 奇偶相位指标

对每帧计算一个质量序列 (q_k)，例如：

* Laplacian 清晰度；
* 梯度能量；
* 边缘总长度；
* UI 模板相似度；
* OCR 置信度；
* 人物轮廓面积；
* 武器边缘长度；
* 运动补偿残差。

定义奇偶相位能量：

[
E_{\pi}
=======

\frac{
\left|
\sum_k(-1)^k q_k
\right|
}{
\sum_k|q_k|+\epsilon
}
]

该值越大，说明原始帧和生成帧之间存在越明显的交替。

还可以直接比较：

[
E_{\text{parity}}
=================

\frac{
|\operatorname{mean}(q_{\text{even}})
-------------------------------------

\operatorname{mean}(q_{\text{odd}})|
}{
\operatorname{mean}(|q_{\text{even}}|)+\epsilon
}
]

## 2. 运动补偿频谱

不能直接对原始像素做 FFT，因为正常移动会产生大量频率能量。

正确做法是：

1. 先对齐到同一物体或同一背景坐标；
2. 对运动补偿后的像素、边缘或深度轨迹做时序 DCT/FFT；
3. 去除低频趋势；
4. 统计高频残差和奇偶相位能量。

建议关注：

* 近 60 Hz 的奇偶交替；
* 15～30 Hz 的武器、细线闪烁；
* 低频的轮廓漂移；
* 局部区域的周期性消失与恢复。

## 3. 多时间间隔差分

建议同时计算：

[
D_1(k)=D(Y_k,Y_{k+1})
]

[
D_2(k)=D(Y_k,Y_{k+2})
]

[
D_4(k)=D(Y_k,Y_{k+4})
]

其中均应进行运动补偿。

`lag=2` 尤其重要，因为它分别连接：

* 原始帧子序列；
* 生成帧子序列。

如果原始帧序列稳定，而生成帧子序列明显抖动，通常可以直接定位为插帧问题，而不是源视频问题。

---

# 七、核心指标五：边缘、轮廓和拓扑完整性

很多插帧坏例并不表现为大面积像素误差，而表现为：

* 头部边缘缺了一块；
* 腿部突然变细或断开；
* 柱子中间断裂；
* 剑尖在一帧中消失；
* 文字笔画发生粘连；
* UI 圆环轮廓不完整。

因此需要单独设计结构完整性分支。

## 1. 端点边缘支持

先提取端点多尺度边缘：

[
E_0=\operatorname{Edge}(X_i)
]

[
E_1=\operatorname{Edge}(X_{i+1})
]

根据半程运动将两侧边缘投影到中间时刻，形成软支持图：

[
S_m=
\operatorname{Warp}(E_0)
\cup
\operatorname{Warp}(E_1)
]

然后与生成帧边缘 (E_m) 比较：

* 边缘召回率：判断模糊或结构缺失；
* 边缘精确率：判断重影和虚假双边；
* Boundary Chamfer Distance；
* 边缘方向差；
* 轮廓连续长度；
* 连通分量数量；
* 骨架端点数量。

典型解释如下：

* 边缘召回率下降：模糊、头部/腿部缺失、细物体消失；
* 边缘精确率下降：重影、双轮廓；
* 连通分量增加：撕裂、破碎；
* 骨架长度交替变化：剑尖或柱子闪烁；
* 边界 Chamfer 突变：人物身周抖动。

## 2. 不要全图平均

人物、武器、文字、UI 占整帧面积很小，全图平均后几乎不会影响总分。

因此应按实例聚合：

[
A_{\text{thin}}
===============

\frac1K
\sum_{j=1}^{K}
A_{\text{thin},j}
]

而不是按像素面积加权。一个小剑尖实例应与一个大 UI 实例各自贡献一次，而不是让背景像素淹没它们。

---

# 八、针对具体坏例的专门评测分支

## 1. 大幅旋转、背景丢失、撕裂和模糊

首先从背景点估计全局相机运动：

* 平移或小角度使用 affine；
* 明显旋转使用 homography；
* 大视差场景增加深度分层；
* 人物、UI 和前景对象应从全局运动估计中剔除。

然后将光流分解为：

[
F_{\text{residual}}
===================

F_{\text{dense}}-F_{\text{camera}}
]

重点检测：

### 背景丢失

通过端点前向投影构造中间时刻的可见性覆盖图：

* 两侧端点均支持的区域；
* 仅左侧支持的区域；
* 仅右侧支持的区域；
* 两侧都不支持的未知区域。

对于理论上有端点支持、但生成帧结构消失的区域，计算：

* 支持边缘召回；
* 纹理能量损失；
* 局部特征匹配失败率；
* 大面积低频涂抹区域。

两侧都不支持的区域不应直接判错，而应降低该区域的评测置信度。

### 背景撕裂

计算残余光流的：

* 梯度；
* 散度；
* 旋度；
* 局部 Jacobian；
* 组合光流突变；
* 边缘跨区域不连续。

对于位移场 (F)，局部映射近似为：

[
J=I+\nabla F
]

若非遮挡区域出现异常接近零、负值或剧烈变化的 (\det(J))，通常意味着局部折叠、拉裂或错误形变。

散度指标适合作为低成本粗筛，但原始光流散度在缩放和旋转场景中会产生误报，因此必须先减去全局相机运动。近期工作显示，简单的光流散度特征可以以明显低于 FloLPIPS 的成本获得相当一部分时序失真检测能力，因此更适合作为风险触发器，而不是最终质量指标。([arXiv][5])

### 背景模糊

在运动补偿后比较：

[
R_{\text{sharp}}
================

\frac{
\operatorname{GradEnergy}(M_i)
}{
\frac12[
\operatorname{GradEnergy}(\hat X_{i\rightarrow m})
+
\operatorname{GradEnergy}(\hat X_{i+1\rightarrow m})
]+\epsilon
}
]

需要分纹理区域统计，不能在天空或纯色区域计算。

对于高视差和多层背景，仅在高风险窗口运行深度估计。Video Depth Anything 专门面向长视频时序一致深度，其轻量版本可作为深度分层候选；不建议全视频默认运行。([arXiv][6])

---

## 2. 人物头部、腿部缺失与身周重影

建议建立一个面向游戏角色的轻量分割模型，输出：

* 人物整体 mask；
* 头部；
* 躯干；
* 手臂；
* 腿部；
* 可选武器区域。

从端点 mask 通过光流投影得到中间时刻软 mask：

[
\hat S_m=
\operatorname{Fuse}
\left(
W(S_0,F_{m\rightarrow0}),
W(S_1,F_{m\rightarrow1})
\right)
]

然后与生成帧分割结果 (S_m) 比较：

* 缺失面积；
* 多余面积；
* 轮廓 Chamfer；
* 连通分量变化；
* 头、腿等关键部件可见性；
* 轮廓厚度异常；
* 双轮廓比例；
* 骨架端点数量变化。

人物身周重影通常表现为：

* 边缘出现两个平行峰；
* 生成帧局部同时与前后端点高度相似；
* 人物 mask 周围出现低透明度附属区域；
* 运动补偿后仍存在与运动方向一致的拖尾。

可以定义前后端点泄漏特征：

[
E_{\text{leak}}
===============

\min
\left(
D(M_i,\hat X_{0\rightarrow m}),
D(M_i,\hat X_{1\rightarrow m})
\right)
]

并结合双边缘检测。单纯相似于一侧端点不一定是错误，但“相似端点纹理 + 双轮廓 + 运动补偿残差”同时出现时，重影置信度会很高。

通用人体姿态模型在二次元、非人形、盔甲和背部武器场景中可能不稳定。更合适的路线是：

1. 使用 SAM 2 等强分割模型离线生成教师标签；
2. 少量人工修正；
3. 蒸馏或训练轻量游戏域分割模型；
4. 生产评测只运行轻量模型；
5. SAM 2 仅用于高风险坏例复核。

SAM 2 支持图像与视频的提示式分割和流式记忆，适合作为标注教师或审核工具，而不是默认逐帧生产分支。([arXiv][7])

---

## 3. 路灯、柱子和细小物体跟随背景

这种问题本质上是**运动层归属错误**。

在端点中分别估计：

* 细物体运动 (v_{\text{obj}})；
* 周围背景运动 (v_{\text{bg}})。

两者的相对运动为：

[
\Delta v_{01}
=============

## v_{\text{obj},01}

v_{\text{bg},01}
]

在生成帧上估计前半段相对运动：

[
\Delta v_{0m}
=============

## v_{\text{obj},0m}

v_{\text{bg},0m}
]

若中间时刻接近 (0.5)，可构造：

[
E_{\text{layer}}
================

\frac{
|2\Delta v_{0m}-\Delta v_{01}|*2
}{
|\Delta v*{01}|_2+\epsilon
}
]

后半程同样计算。

当柱子错误地跟随背景运动时，通常会出现：

[
\Delta v_{0m}\approx0
]

即物体相对于背景的运动差异被错误抹掉。

细物体分支还应包含：

* Line Segment Detector；
* 多尺度 Canny 或 learned edge；
* 骨架化；
* 线段长度；
* 线段方向；
* 骨架连续性；
* 端点数量；
* 线宽变化；
* 局部边缘召回。

这些计算应在原始分辨率 ROI 上执行，而不是把整帧缩放到很小后检测。建议先在低分辨率定位，再裁剪 256～512 像素高分辨率区域精评。

---

## 4. UI、技能轮盘、移动轮盘和文字

UI 是最适合充分利用端点参考的区域。

### 自动识别屏幕空间 UI

游戏 UI 通常具有以下特征：

* 背景发生明显运动时，UI 在屏幕坐标中接近静止；
* 多帧位置高度稳定；
* 边缘和纹理具有持续性；
* 通常位于固定屏幕区域；
* 与相机运动无关。

可以综合：

* 多帧零运动概率；
* 局部模板稳定性；
* 屏幕位置先验；
* 文本检测；
* 圆环、技能图标等形状先验；
* 与背景光流的差异；

生成静态 UI mask。

### 静态 UI

如果两个端点中的 UI 内容一致：

[
D_{\text{UI}}(X_i,X_{i+1})<\delta
]

那么真实中间帧的 UI 基本可以认为也应一致。此时可以直接计算：

[
E_{\text{UI-static}}
====================

D(M_i,X_i)
]

建议使用：

* 原分辨率 L1；
* 梯度误差；
* 边缘 F-score；
* 模板相关性；
* 圆环半径、圆心和厚度；
* 文本骨架；
* 局部感知特征。

这一分支对 UI 模糊、轻微平移、轮廓闪烁、文字粘连非常敏感。

### 动态 UI

如果两个端点的 UI 本身不同，就不能要求生成帧等于任一端点。此时评估：

* 特征是否单调从状态 A 过渡到 B；
* 是否产生多余双影；
* 是否在相邻生成帧之间反复跳变；
* 几何位置是否稳定；
* 是否出现奇偶帧清晰度交替。

OCR 只在以下条件同时满足时启用：

1. 两端 OCR 文本一致；
2. 两端识别置信度足够高；
3. UI 区域没有动画。

否则应优先使用文字边缘和拓扑结构，避免把 OCR 模型自身的不稳定误认为插帧错误。

---

## 5. 剑尖、剑柄和背部武器抖动

武器抖动不能只在屏幕坐标中测量，因为人物和相机本身都在运动。

应跟踪：

* 人物躯干中心 (p_{\text{body},t})；
* 剑尖、剑柄或武器骨架关键点 (p_{\text{weapon},t})。

构造人物相对坐标：

[
r_t=
p_{\text{weapon},t}
-------------------

p_{\text{body},t}
]

然后统计：

[
a_t=r_{t+1}-2r_t+r_{t-1}
]

[
j_t=a_{t+1}-a_t
]

重点检测：

* 生成帧位置上的二阶差分尖峰；
* 奇数帧和偶数帧的相对位置差异；
* 剑身方向突然变化；
* 剑尖边缘间歇性消失；
* 武器轮廓长度交替；
* 武器 mask 面积周期性变化。

对于普通窗口使用轻量局部点跟踪；对于高风险窗口再启用 CoTracker3。CoTracker3 针对长时间点轨迹、遮挡和快速运动进行了设计，适合作为复杂坏例的审核级跟踪器。([arXiv][8])

---

## 6. 卡牌翻转、商店弹出和离散状态切换

这一类必须区分两种情况。

### 连续物理运动

例如卡牌发生真实 3D 翻转，可以检测：

* 卡牌四个角点；
* 四边形面积；
* 宽高比；
* 单应变换；
* 表面纹理方向；
* 翻转角度的单调性。

中间帧应表现为合理的中间几何状态，而不是停留在上一帧或直接跳到下一帧。

### 离散 UI 状态切换

例如点击商店后，UI 在两个源帧之间突然切换。

这种情况下，真实切换时刻可能是：

* (t=0.2)；
* (t=0.5)；
* (t=0.8)。

仅凭两个 60 FPS 端点无法知道。因此不能规定中间帧必须是上一状态、下一状态或二者的某个混合比例。

此时只应惩罚：

* 双重 UI 重影；
* 文字或卡牌局部混合；
* 状态先变后退；
* 多余的中间闪烁；
* 几何形变；
* 同一组件不同区域处于矛盾状态。

这类窗口应输出：

```text
event_ambiguity = high
quality_confidence = low / medium
```

也就是说，系统可以可靠判断“是否出现重影和不稳定”，但不能可靠判断“切换时机是否精确正确”。

---

# 九、学习型和通用视频质量分支

核心几何指标之外，可以增加一个低权重的学习型感知分支。

## 1. FAST-VQA

FAST-VQA 使用空间碎片采样降低高分辨率视频质量评估成本，作者报告其在 1080p 场景中可以大幅减少计算量，因此适合提供低成本的全局技术质量先验。([arXiv][9])

但它不能独立判断：

* 物体是否跟错运动层；
* 人物腿部是否缺失；
* 武器是否相对人物抖动；
* 卡牌状态是否合理。

因此建议权重不超过总体特征的 5%～10%。

## 2. DOVER

DOVER 将视频质量分为技术质量与审美质量。插帧评测应主要使用技术质量分支，不应让画面美术风格、色彩偏好和场景内容显著影响插帧质量分。([arXiv][10])

## 3. VFI 专用无参考模型

VFIPQA 直接使用“前帧—生成帧—后帧”三帧输入，比通用 NR-VQA 更贴合当前任务；2025 年的 VFI 质量评测工作也分别探索了全参考与无参考的运动特征建模。它们适合作为学习型基线或附加特征，但仍需在实际游戏数据上重新标定。([arXiv][11])

---

# 十、三级评测级联：在速度和覆盖率之间平衡

不建议让分割、深度和点跟踪模型逐帧运行。正确方案是：

> 全视频低成本扫描，核心模型处理少量代表片段，重模型只处理高风险区域。

## 第一级：全帧低成本扫描

把视频缩放到宽 320～480，逐帧计算：

* 感知哈希；
* 亮度与颜色统计；
* Laplacian 清晰度；
* 边缘能量；
* 帧重复概率；
* 奇偶清晰度差；
* 粗粒度块运动；
* 全局旋转风险；
* 场景切换概率；
* UI 区域变化；
* 高边缘密度和细线风险。

形成每个时间窗口的风险值：

[
R_w=
\sum_j \lambda_j z_{j,w}
]

其中使用稳健标准化：

[
z_{j,w}
=======

\frac{
e_{j,w}-\operatorname{median}(e_j)
}{
1.4826\cdot\operatorname{MAD}(e_j)+\epsilon
}
]

这样可以根据当前视频内容自动适应，而不是使用大量固定阈值。

## 第二级：标准核心评测

建议的初始采样配置：

### Fast 模式

* 8 个均匀窗口；
* 8 个最高风险窗口；
* 每个窗口 5 帧；
* 光流和核心时序指标；
* 不运行深度和大型分割。

### Standard 模式

* 16 个均匀窗口；
* 最多 32 个高风险窗口；
* 每个窗口 5 帧；
* 光流宽度约 800～960；
* 运行轻量 UI、人物和细边缘分支。

高风险窗口选择时应进行时间维度 NMS，例如相邻峰值之间至少间隔 0.5 秒，避免 32 个窗口全部集中在同一个坏例附近。

对于一个 60 秒、120 FPS 的视频，共 7200 帧。Standard 模式若选取 48 个五帧窗口，核心重计算只覆盖约：

[
\frac{48\times5}{7200}\approx3.3%
]

的帧。全视频仍由低成本扫描保证短时坏例不被完全漏掉。

## 第三级：审核级诊断

只对第二级中最高风险的 1%～10% 窗口运行：

* CoTracker3；
* SAM 2 复核；
* Video Depth Anything；
* 全分辨率边缘和骨架；
* 复杂人物部位分割；
* 武器关键点跟踪；
* 卡牌四边形追踪。

建议设置硬上限，例如每段视频最多 64 或 256 个审核窗口，使运行时间始终可控。

ReVQ-2k 的方案采用固定数量的五帧片段和中等分辨率裁剪进行评测，但其论文也报告完整推理仍可能超过十秒。生产系统应进一步通过风险采样、源端缓存和按需重模型降低成本。([arXiv][2])

---

# 十一、评分融合与最终报告

## 1. 不要直接平均所有帧

插帧坏例往往是短暂而严重的。如果简单平均，一个持续 3 帧的明显撕裂会被数千个正常帧稀释。

对每个类别 (c)，建议使用中位数和尾部分位数组合：

[
A_c=
0.4P_{50}(e_c)
+
0.4P_{90}(e_c)
+
0.2P_{99}(e_c)
]

其中：

* (P_{50}) 表示整体平均体验；
* (P_{90}) 表示常见坏例；
* (P_{99}) 表示极端严重坏例。

对于武器、文字和 UI，应先按实例聚合，再跨实例计算分位数。

## 2. 输出多维分数

建议至少输出：

| 分数                       | 含义              |
| ------------------------ | --------------- |
| Motion Consistency       | 光流组合、轨迹和运动层归属   |
| Temporal Stability       | 运动补偿残差、闪烁和奇偶交替  |
| Structural Integrity     | 边缘、轮廓、骨架和拓扑完整性  |
| Character Integrity      | 人物身体部位与身周重影     |
| Thin Object / Weapon     | 柱子、剑尖、剑柄等稳定性    |
| UI / Text                | 技能轮盘、移动轮盘、文字稳定性 |
| Transition Quality       | 卡牌、商店和状态切换      |
| Global Technical Quality | 全局模糊、噪声、压缩和观感   |
| Confidence               | 当前判断的可靠程度       |

## 3. 初始总分公式

在尚未训练融合模型前，可以先使用：

[
Q=
100\exp
\left[
------

\left(
0.25A_{\text{motion}}
+
0.20A_{\text{temporal}}
+
0.20A_{\text{structure}}
+
0.15A_{\text{character/thin}}
+
0.10A_{\text{UI}}
+
0.05A_{\text{transition}}
+
0.05A_{\text{global}}
\right)
\right]
]

这些权重只能作为启动配置，不能作为最终标准。

## 4. 最终使用单调融合模型

建议最终使用：

* 带单调约束的 LightGBM；
* 或小型单调网络；
* 外加 isotonic calibration。

单调约束应保证：

* 撕裂误差增加不能提高分数；
* UI 闪烁增加不能提高分数；
* 人物缺失增加不能提高分数；
* 置信度低不能被解释为高质量证据。

训练目标可以写为：

[
L=
\lambda_{\text{rank}}L_{\text{pair-rank}}
+
\lambda_{\text{MOS}}L_{\text{MOS}}
+
\sum_c\lambda_cL_{\text{artifact},c}
+
\lambda_uL_{\text{uncertainty}}
]

其中成对排序损失：

[
L_{\text{pair-rank}}
====================

\log
\left[
1+
\exp\left(
-y(Q_A-Q_B)
\right)
\right]
]

对于实际模型比较，A/B 成对偏好通常比让标注者给出精确的 0～100 分更稳定。

## 5. 同时给出绝对分和同源相对分

应该输出两种结果：

### 绝对质量分

用于不同视频、不同游戏之间的大致统计，但更容易受到内容难度影响。

### 同源相对分

在同一个原始 60 FPS 视频上比较多个插帧模型，这一排序通常更可靠，因为内容、运动难度、场景结构都相同。

渲染视频质量研究也观察到了内容和运动分布带来的偏差，因此同内容、同运动条件下的模型比较应作为主要使用场景。([arXiv][2])

---

# 十二、评测数据和标定方案

仅依赖公开数据不能完全覆盖游戏插帧坏例，需要构建自己的标定集。

## 1. 高帧率伪 Ground Truth 数据

采集真实 120 FPS 或 240 FPS 游戏视频：

1. 从 120 FPS 中隔帧取样成 60 FPS；
2. 使用候选模型恢复到 120 FPS；
3. 被删除的真实中间帧就是 Ground Truth；
4. 同时计算真正的全参考指标和拟议的端点参考指标；
5. 训练两者之间的映射。

这种数据可以直接验证：

* 光流组合误差是否真的对应重建误差；
* 奇偶频率是否能发现闪烁；
* 边缘召回是否对应人物或武器缺失；
* 反向锚点闭环是否与真实中间帧误差相关。

## 2. 真实 60→120 无 Ground Truth 数据

收集实际部署场景，包括：

* 大幅旋转；
* 高视差场景；
* 人物快速移动；
* 武器和复杂地面；
* 柱子、路灯和栅栏；
* 技能 UI 和文字；
* 卡牌翻转；
* 商店和菜单切换。

这部分通过人工 A/B 排序和坏例标签标定。

## 3. 合成坏例

可以在真实高帧率数据上人为制造：

* 仅奇数帧模糊；
* 前后帧 Alpha 混合重影；
* 局部流场撕裂；
* 人物 mask 挖洞；
* 剑尖间歇删除；
* 细线跟随背景移动；
* UI 亚像素抖动；
* 文字模糊；
* 局部亮度闪烁；
* 状态切换双重叠加。

合成数据适合训练“问题定位能力”，但最终权重必须在真实模型输出上标定，否则模型容易只学会合成缺陷的模式。

## 4. 人工标注协议

建议每个样本使用 3～5 秒同步循环视频，标注内容包括：

* A/B 总体偏好；
* 运动一致性 0～3；
* 闪烁 0～3；
* 人物完整性 0～3；
* 细物体和武器 0～3；
* UI 和文字 0～3；
* 转场质量 0～3；
* 是否属于事件歧义。

每个样本至少 3 位标注者。

数据划分必须按：

* 游戏；
* 场景；
* 插帧模型；
* 运动类型；

进行隔离，不能随机把同一段视频的相邻裁剪同时分到训练集和测试集。ReVQ-2k 也使用了场景独立的数据划分思想。([arXiv][2])

公开数据方面，可以使用：

* BVI-VFI 作为插帧主观质量基础；
* ReVQ-2k 作为渲染视频和时序稳定性预训练；
* VFIPQA 相关三帧数据作为 triplet 感知分支基线。

BVI-VFI 包含数百条插帧失真视频和大规模主观评分；ReVQ-2k 包含 2000 条渲染视频及超过五万条主观标注，但二者仍无法替代自有游戏场景标注。([arXiv][12])

## 5. 验收指标

评测器本身应使用以下指标验收：

* SRCC：与主观排序的一致性；
* PLCC：与主观绝对分的一致性；
* A/B Pairwise Accuracy；
* 各坏例类别的 F1 或 AP；
* 最差 10% 片段的召回率；
* Leave-One-Game-Out 泛化；
* Leave-One-Model-Out 泛化；
* Fast 模式与 Audit 模式的排序一致性；
* 单视频运行时间和峰值显存。

建议作为工程目标，而不是先验保证：

* 同源模型 A/B 排序准确率达到 80% 以上；
* 最差 10% 坏例召回率达到 90% 以上；
* 留出游戏上的 SRCC 达到 0.8 左右；
* Fast 模式与 Audit 模式的模型级排序大体一致。

---

# 十三、推荐的代码模块划分

```text
rr_vfiqa/
├── io/
│   ├── video_reader.py
│   ├── timestamp_alignment.py
│   └── color_normalization.py
│
├── cache/
│   ├── source_cache.py
│   ├── feature_store.py
│   └── cache_schema.py
│
├── sampling/
│   ├── cheap_scan.py
│   ├── risk_score.py
│   ├── temporal_nms.py
│   └── window_selector.py
│
├── motion/
│   ├── flow_estimator.py
│   ├── occlusion.py
│   ├── global_camera_motion.py
│   ├── flow_composition.py
│   └── flow_geometry.py
│
├── metrics/
│   ├── anchor_integrity.py
│   ├── cycle_reconstruction.py
│   ├── temporal_compensation.py
│   ├── parity_frequency.py
│   ├── edge_structure.py
│   └── global_technical_quality.py
│
├── regions/
│   ├── ui_detector.py
│   ├── character_segmenter.py
│   ├── thin_object_detector.py
│   ├── weapon_tracker.py
│   ├── text_evaluator.py
│   └── transition_evaluator.py
│
├── models/
│   ├── flow_backend.py
│   ├── tracker_backend.py
│   ├── depth_backend.py
│   ├── segmentation_backend.py
│   └── vqa_backend.py
│
├── fusion/
│   ├── feature_normalizer.py
│   ├── monotonic_calibrator.py
│   ├── confidence.py
│   └── score_schema.py
│
└── report/
    ├── timeline_report.py
    ├── heatmap_renderer.py
    ├── badcase_exporter.py
    └── json_report.py
```

核心调用接口建议设计为：

```python
report = evaluate_vfi(
    source_video="source_60fps.mp4",
    candidate_video="candidate_120fps.mp4",
    preset="standard",
    cache_dir="./cache",
    device="cuda",
)
```

返回结构：

```python
{
    "overall_score": 82.6,
    "confidence": 0.91,
    "scores": {
        "motion_consistency": 78.3,
        "temporal_stability": 84.1,
        "structural_integrity": 80.5,
        "character_integrity": 76.2,
        "thin_object_weapon": 69.4,
        "ui_text": 92.3,
        "transition_quality": 88.0,
        "global_technical_quality": 86.7
    },
    "worst_windows": [
        {
            "start": 12.533,
            "end": 12.700,
            "type": ["thin_object_stick", "background_tear"],
            "severity": 0.91,
            "confidence": 0.94,
            "boxes": [...]
        }
    ]
}
```

整体执行逻辑可以概括为：

```python
source_cache = preprocess_source(source_video)

candidate_meta = align_candidate(
    source_video,
    candidate_video,
)

cheap_features = scan_all_frames(
    candidate_video,
    source_cache,
)

windows = select_windows(
    cheap_features,
    uniform_count=16,
    risk_count=32,
    temporal_nms_seconds=0.5,
)

features = []

for batch in batch_windows(windows):
    features.extend(
        compute_flow_composition(batch, source_cache)
    )
    features.extend(
        compute_motion_compensated_temporal_metrics(batch)
    )
    features.extend(
        compute_parity_and_frequency_metrics(batch)
    )
    features.extend(
        compute_reverse_anchor_cycle(batch, source_cache)
    )
    features.extend(
        compute_edge_structure_metrics(batch, source_cache)
    )

high_risk_regions = select_high_risk_regions(features)

for batch in batch_regions(high_risk_regions):
    features.extend(
        compute_character_ui_thin_object_diagnostics(
            batch,
            source_cache,
        )
    )

report = calibrator.predict_and_localize(features)
```

为了同时支持 CUDA 和 NPU，评测逻辑不应直接依赖 `.cuda()`，而应让光流、分割、跟踪和深度模型都实现统一 backend 接口。分辨率采用有限桶，例如：

```text
480p / 720p / 960-width / native ROI
```

避免大量动态 shape，源端特征可使用 float16 光流、uint8 mask 和内存映射缓存。

---

# 十四、落地优先级

## P0：先完成可用 MVP

第一版只实现：

1. 时间戳和偶数锚点对齐；
2. 源端光流、遮挡和相机运动缓存；
3. 双向光流组合一致性；
4. 运动补偿时序残差；
5. 奇偶帧清晰度与频率指标；
6. 多尺度边缘召回和双轮廓检测；
7. 反向锚点闭环；
8. 全帧风险扫描；
9. 最差片段导出和时间轴报告。

这一版已经能够覆盖大部分：

* 背景撕裂；
* 旋转模糊；
* 局部冻结；
* 人物身周重影；
* 奇偶帧闪烁；
* 细物体消失；
* UI 模糊。

## P1：增加游戏特定语义分支

增加：

* 静态 UI 自动识别；
* 人物轻量分割；
* 细线和柱子检测；
* 武器相对人物跟踪；
* 文字稳定性；
* 卡牌和商店事件分类。

## P2：建立自有数据和融合模型

使用：

* 高帧率伪 Ground Truth；
* 真实无 Ground Truth 游戏数据；
* 合成坏例；
* 人工 A/B 排序；

训练单调融合器，并确定正式权重和阈值。

## P3：生产优化

完成：

* 多模型共享源缓存；
* CUDA/NPU backend；
* 混合精度和批量推理；
* 高风险窗口限制；
* 分辨率桶；
* 评测版本管理；
* 模型回归看板；
* 同源多模型排名与坏例对比。

---

# 最终建议

这套评测系统的核心应固定为：

> **端点光流组合与遮挡一致性 + 运动补偿多频时序稳定性 + 反向锚点闭环 + 局部结构完整性。**

人物、武器、细杆、UI、文字和卡牌等语义分支只在高风险区域运行；FAST-VQA、DOVER 或 VFI 专用 NR 模型作为低权重感知先验；最终通过高帧率伪 GT 和真实游戏人工排序进行标定。

这样既不会把问题退化成一个不可靠的通用“无参考视频质量分”，又能把背景撕裂、人物缺失、武器闪烁、细物体跟随错误、UI 抖动和离散事件劣化分别定位出来，同时把重模型的计算限制在全视频很小的一部分。

[1]: https://arxiv.org/abs/2202.07727 "https://arxiv.org/abs/2202.07727"
[2]: https://arxiv.org/html/2510.13349v1 "https://arxiv.org/html/2510.13349v1"
[3]: https://arxiv.org/abs/2405.14793 "https://arxiv.org/abs/2405.14793"
[4]: https://arxiv.org/abs/1906.05928 "https://arxiv.org/abs/1906.05928"
[5]: https://arxiv.org/html/2508.09078v1 "https://arxiv.org/html/2508.09078v1"
[6]: https://arxiv.org/abs/2501.12375 "https://arxiv.org/abs/2501.12375"
[7]: https://arxiv.org/abs/2408.00714 "https://arxiv.org/abs/2408.00714"
[8]: https://arxiv.org/html/2410.11831v1 "https://arxiv.org/html/2410.11831v1"
[9]: https://arxiv.org/abs/2207.02595 "https://arxiv.org/abs/2207.02595"
[10]: https://arxiv.org/abs/2211.04894 "https://arxiv.org/abs/2211.04894"
[11]: https://arxiv.org/html/2312.15659v1 "https://arxiv.org/html/2312.15659v1"
[12]: https://arxiv.org/abs/2210.00823 "https://arxiv.org/abs/2210.00823"
